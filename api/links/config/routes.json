{
  "routes": [
    {
      "method": "GET",
      "path": "/links",
      "handler": "links.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/links",
      "handler": "links.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/links",
      "handler": "links.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
